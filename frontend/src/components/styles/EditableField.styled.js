import styled from 'styled-components'

export const StyledEditableField = styled.div`
    display: flex;
    align-items: center;
    gap: 8px;

    .clickable-icon {
        cursor: pointer;
    }

    .placeholder {
        font-style: italic;
    }
`